[validate_data]
data_root = "/data/public_data/mllmbenchmark"

[build_chain_manifest]
data_root = "/data/public_data/mllmbenchmark"
out = "../../artifacts/chain_pairs.jsonl"

# Use `prepare_main.toml` for the built-in `main` protocol.
# Use `prepare_custom_protocol.toml` as a starting point for model-native sampling protocols.
# The example prepare configs generate sampled frames and sampled videos.
